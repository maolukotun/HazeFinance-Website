# Copy to .env.local (bun/vite auto-loads it) and adjust as needed.
# URL of the haze-backend API — see ../haze-backend/README.md.
# Defaults to http://localhost:8402 if unset (see src/routes/dashboard.tsx).
VITE_HAZE_API_URL=http://localhost:8402
