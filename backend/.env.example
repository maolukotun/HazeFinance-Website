# Copy to .env and fill in for anything beyond local dev.
# Local dev works with zero configuration — defaults are in scripts/dev.js.

PORT=8402
SEED_WALLET_COUNT=40
QUERY_PRICE_USDC=0.02
CORS_ORIGIN=*

# Needed once you wire up real chain reads / registry syncing (not done
# in this scaffold — see src/registryBridge/syncWeights.js). Get current
# values from Nick.
ROBINHOOD_RPC_URL=
HAZE_PROFILE_REGISTRY_ADDRESS=
HAZE_SPLITTER_ADDRESS=
